{
  "name": "auto-compiler",
  "version": "1.0.0",
  "description": "Auto-compile C++ files on change",
  "scripts": {
    "watch": "npx nodemon --exec \"g++ -o output main.cpp && ./output\" --ext cpp,h --ignore output",
    "start": "g++ -o output main.cpp && ./output"
  },
  "dependencies": {
    "nodemon": "^3.1.9"
  }
}
